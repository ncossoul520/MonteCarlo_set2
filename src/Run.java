public class Run {
    static final int NUM_RUNS = 10000;

    public static void main(String[] args) {
//        Triangle.Run( NUM_RUNS );
//        Children.Run( NUM_RUNS );
//        Pepy.Run( NUM_RUNS );
        Gambler.Run( NUM_RUNS );
    }
}
